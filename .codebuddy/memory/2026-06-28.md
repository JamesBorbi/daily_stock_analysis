# 2026-06-28

## 反向分析策略训练 - 完整落地

### 核心资产
- **前端**: `ReverseTrainingPage.tsx` (`/reverse-training`)，含统计条、分页表格、新增/编辑/删除、扫描/对比/训练按钮
- **后端**: `api/v1/endpoints/reverse_training.py` (9端点), `src/repositories/reverse_repo.py` (数据仓库)
- **扫描引擎**: `scripts/screen_reverse.py` (全市场扫描+NaN防护)
- **策略**: `strategies/reverse_engineered.yaml` (已部署)
- **数据**: `data/reverse/history.csv` (215条,8列完整格式,纳入Git), `data/stock_analysis.db` (SQLite,表reverse_picks)

### 数据同步机制
- 增删改操作自动同步 CSV (纳入Git版本控制)
- 新电脑 clone 后点「初始化数据」一键恢复
- CSV 格式: date,stock_code,stock_name,pick_price,prev_close,pick_day_change,next_day_change,success

### 新增弹窗
- 内联展开（非modal），直接在新增按钮下方
- 支持从Excel粘贴7列制表符分隔数据，自动识别日期和隔日涨跌
- 日期用浏览器原生 type=date 选择器

### 页面最终布局
- 顶部: 标题 + [扫描][日期][对比][训练] 按钮行
- 二行: [新增]按钮 + 数据就绪标识
- 下方: 内联统计条 → 扫描/对比结果区 → 搜索栏 → 分页表格(20条/页)

### 选股逻辑规则 (screen_reverse.py)
- 趋势强度>=30, 价格乖离MA5在-3%~+5%, 量比0.5-2.5, 排除MACD死叉/ST/涨停/PE>200
- 已添加NaN防护和枚举安全兜底

### 关键技术决策
- AlphaSift 归属外部包，反向分析策略走 DSA 内部引擎
- 前端驼峰/蛇形自动转换 (toCamelCase/toSnakeCase)
- 新电脑首次运行显示「初始化数据」按钮，有数据后显示「数据已就绪」
